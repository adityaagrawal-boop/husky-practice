# Feature Map - `<route>`

> Generated by walking `references/feature-mapping.md` algorithm through `<path/to/route/component>` and every sibling component it imports.
> Per the skill's edit/detail folding rule (`references/split-criteria.md`), edit/detail destinations are folded into their parent feature.
> Each node is a distinct feature surface that gets its own folder (`README.md` + `metadata.json`) when documented.

**Source commit:** `<short-hash>`
**Generated:** `YYYY-MM-DD`
**Tier:** `<Critical | Important | Standard>` (inherits from SURFACE-MAP.md; per-feature overrides noted on each node)

---

## Tree

```
/<route>  (route-hub)
│
├── 1. <feature-slug>  (<source-of-truth: tab N / modal / paywall / etc.>)
│   ├── 1.1 <sub-feature-slug>     (<one-line description with code reference>)
│   ├── 1.2 <sub-feature-slug>     (<...>)
│   └── ...
│
├── 2. <feature-slug>
│   ├── 2.1 <sub-feature-slug>
│   ├── 2.2 <sub-feature-slug>
│   ├── 2.X row-editor  (opens as separate route /<edit-route>; <variant-name> variant: N tabs)
│   │   ├── 2.X.1 <editor-sub-feature>
│   │   ├── 2.X.2 <editor-sub-feature>
│   │   └── ...
│   └── ...
│
└── ...
```

Indent depth reflects parent-child relationship. Number prefixes scope each path. Sub-features that are atomic (per `references/feature-mapping.md` Step 3) get documented inline in their parent's README, not as separate folders.

---

## Per-node reasons

For each top-level node, one short paragraph: why it's its own feature, what end-user task it serves, what distinguishes it from siblings.

### 1. `<feature-slug>`
<2-4 sentences: distinct surface, distinct end-user task, distinct gating, why it cannot be collapsed into a sibling>

### 2. `<feature-slug>`
<...>

### ... (one per top-level node)

---

## Variation matrix (if edit/detail folding applied per-parent)

When the same component renders differently per parent, show the matrix here so the per-parent variation is visible at a glance.

| Sub-feature inside row-editor | parent A | parent B | parent C | parent D |
|---|---|---|---|---|
| sub-feature-1 | ✅ | ✅ | ❌ | ❌ |
| sub-feature-2 | ✅ (Tier-3+ gate) | ✅ (Tier-3+ gate) | ✅ | ✅ |
| ... | ... | ... | ... | ... |
| **Total** | **N tabs** | **N tabs** | **N tabs** | **N tabs** |

---

## Sanity-count check

Per `references/feature-mapping.md` Step 7, verify:

- CommonTable usages: `<count>`. Required ≥ 3 feature nodes per usage → `<status>`.
- Paywall variants: `<count>`. Required ≥ 2 nodes per variant → `<status>`.
- `<Tabs>` items: `<count>`. Required ≥ 1 + (tab count) top-level features → `<status>`.
- Edit/detail folding applied: `<yes/no>`.

If any check fails, re-walk Step 1.

---

## Open questions

Items where the skill couldn't determine the answer from code alone, or where confidence is LOW. To verify during per-feature documentation.

1. `<question>`
2. `<question>`
3. `<question>`

---

## Folder layout to create

```
screens/<route>/
├── README.md                       (route-hub, thin)
├── FEATURE-MAP.md                  (this file)
├── metadata.json                   (route-level identity)
├── <feature-slug-1>/
│   ├── README.md
│   ├── metadata.json
│   ├── <sub-feature-slug>/
│   └── ...
├── <feature-slug-2>/
│   └── ...
└── ...
```

Each feature node in the tree maps to a folder. Atomic sub-features documented inline in their parent's README; no separate folder.

---

## Next steps

1. Create folders for top-level nodes 1-N.
2. Create per-parent row-editor sub-folders (if applicable).
3. Document each feature one at a time via `references/feature-first-reasoning.md` (hypothesis → confirm → edge cases → README + metadata).
4. Quality review per `references/quality-review.md` once all features have READMEs.
